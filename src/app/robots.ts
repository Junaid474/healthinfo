import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    sitemap: 'https://healthinfo-blog.com/sitemap.xml', // TODO: Replace with your actual domain
  };
}
